# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.31.1
#   sqlc-gen-better-python v0.8.0
# source file: queries_triple_quote.sql
"""Module containing queries from file queries_triple_quote.sql."""

from __future__ import annotations

__all__: collections.abc.Sequence[str] = (
    "get_both_quotes_pattern",
    "get_triple_quote_pattern",
)

import typing

if typing.TYPE_CHECKING:
    import collections.abc
    import sqlite3


GET_TRIPLE_QUOTE_PATTERN: typing.Final[str] = '''-- name: GetTripleQuotePattern :one

SELECT 'a"""b' AS pattern FROM test_slice WHERE id = ?
'''

GET_BOTH_QUOTES_PATTERN: typing.Final[str] = "-- name: GetBothQuotesPattern :one\nSELECT 'a\"\"\"b''''''c' AS pattern FROM test_slice WHERE id = ?\n"


def get_triple_quote_pattern(conn: sqlite3.Connection, *, id_: int) -> str | None:
    '''Fetch one from the db using the SQL query with `name: GetTripleQuotePattern :one`.

    ```sql

    SELECT 'a"""b' AS pattern FROM test_slice WHERE id = ?
    ```

    Args:
        conn:
            Connection object of type `sqlite3.Connection` used to execute the query.
        id_: int.

    Returns:
        Result of type `str` fetched from the db. Will be `None` if not found.
    '''
    row = conn.execute(GET_TRIPLE_QUOTE_PATTERN, (id_,)).fetchone()
    if row is None:
        return None
    return row[0]


def get_both_quotes_pattern(conn: sqlite3.Connection, *, id_: int) -> str | None:
    """Fetch one from the db using the SQL query with `name: GetBothQuotesPattern :one`.

    Args:
        conn:
            Connection object of type `sqlite3.Connection` used to execute the query.
        id_: int.

    Returns:
        Result of type `str` fetched from the db. Will be `None` if not found.
    """
    row = conn.execute(GET_BOTH_QUOTES_PATTERN, (id_,)).fetchone()
    if row is None:
        return None
    return row[0]
