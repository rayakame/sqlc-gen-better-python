# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.31.1
#   sqlc-gen-better-python v0.8.0
# source file: queries.sql
# pyright: reportUnknownMemberType=false
"""Module containing queries from file queries.sql."""

from __future__ import annotations

__all__: collections.abc.Sequence[str] = (
    "QueryResults",
    "all_mysql_types_cursor",
    "count_mysql_types",
    "delete_one_mysql_type",
    "get_exec_last_id_name",
    "get_many_bool",
    "get_many_date",
    "get_many_decimal",
    "get_many_inner_mysql_type",
    "get_many_mood",
    "get_many_mysql_type",
    "get_many_nullable_inner_mysql_type",
    "get_many_time",
    "get_one_bit",
    "get_one_blob",
    "get_one_bool",
    "get_one_date",
    "get_one_datetime",
    "get_one_decimal",
    "get_one_inner_mysql_type",
    "get_one_json",
    "get_one_mood",
    "get_one_mysql_type",
    "get_one_tag",
    "get_one_time",
    "get_one_year",
    "get_reserved_arg",
    "get_type_override",
    "insert_exec_last_id",
    "insert_one_inner_mysql_type",
    "insert_one_mysql_type",
    "insert_reserved_arg",
    "insert_type_override",
    "list_months",
    "touch_exec_last_id",
    "update_varchar_test",
)

from collections import UserString
import operator
import typing

if typing.TYPE_CHECKING:
    import asyncmy
    import asyncmy.cursors
    import collections.abc
    import datetime
    import decimal

    type QueryResultsArgsType = int | float | str | memoryview | bytes | decimal.Decimal | datetime.date | datetime.time | datetime.datetime | datetime.timedelta | collections.abc.Sequence[QueryResultsArgsType] | None

from test.driver_asyncmy.pydantic.functions import enums
from test.driver_asyncmy.pydantic.functions import models


INSERT_ONE_MYSQL_TYPE: typing.Final[str] = """-- name: InsertOneMysqlType :exec
INSERT INTO test_mysql_types (
    id, int_test, integer_test, mediumint_test, smallint_test, tinyint_test, bigint_test,
    int_unsigned_test, bigint_unsigned_test, year_test,
    tinyint1_test, bool_test, boolean_test,
    float_test, double_test, double_precision_test, real_test,
    decimal_test, numeric_test,
    char_test, varchar_test, tinytext_test, text_test, mediumtext_test, longtext_test,
    binary_test, varbinary_test, tinyblob_test, blob_test, mediumblob_test, longblob_test, bit_test,
    date_test, datetime_test, datetime6_test, timestamp_test, time_test,
    json_test, mood, tag
) VALUES (
             %s, %s, %s, %s, %s, %s, %s,
             %s, %s, %s,
             %s, %s, %s,
             %s, %s, %s, %s,
             %s, %s,
             %s, %s, %s, %s, %s, %s,
             %s, %s, %s, %s, %s, %s, %s,
             %s, %s, %s, %s, %s,
             %s, %s, %s
         )
"""

INSERT_ONE_INNER_MYSQL_TYPE: typing.Final[str] = """-- name: InsertOneInnerMysqlType :exec
INSERT INTO test_inner_mysql_types (
    table_id, int_test, integer_test, mediumint_test, smallint_test, tinyint_test, bigint_test,
    int_unsigned_test, bigint_unsigned_test, year_test,
    tinyint1_test, bool_test, boolean_test,
    float_test, double_test, double_precision_test, real_test,
    decimal_test, numeric_test,
    char_test, varchar_test, tinytext_test, text_test, mediumtext_test, longtext_test,
    binary_test, varbinary_test, tinyblob_test, blob_test, mediumblob_test, longblob_test, bit_test,
    date_test, datetime_test, datetime6_test, timestamp_test, time_test,
    json_test, mood, tag
) VALUES (
             %s, %s, %s, %s, %s, %s, %s,
             %s, %s, %s,
             %s, %s, %s,
             %s, %s, %s, %s,
             %s, %s,
             %s, %s, %s, %s, %s, %s,
             %s, %s, %s, %s, %s, %s, %s,
             %s, %s, %s, %s, %s,
             %s, %s, %s
         )
"""

GET_ONE_MYSQL_TYPE: typing.Final[str] = """-- name: GetOneMysqlType :one
SELECT id, int_test, integer_test, mediumint_test, smallint_test, tinyint_test, bigint_test, int_unsigned_test, bigint_unsigned_test, year_test, tinyint1_test, bool_test, boolean_test, float_test, double_test, double_precision_test, real_test, decimal_test, numeric_test, char_test, varchar_test, tinytext_test, text_test, mediumtext_test, longtext_test, binary_test, varbinary_test, tinyblob_test, blob_test, mediumblob_test, longblob_test, bit_test, date_test, datetime_test, datetime6_test, timestamp_test, time_test, json_test, mood, tag FROM test_mysql_types WHERE id = %s
"""

GET_ONE_INNER_MYSQL_TYPE: typing.Final[str] = """-- name: GetOneInnerMysqlType :one
SELECT table_id, int_test, integer_test, mediumint_test, smallint_test, tinyint_test, bigint_test, int_unsigned_test, bigint_unsigned_test, year_test, tinyint1_test, bool_test, boolean_test, float_test, double_test, double_precision_test, real_test, decimal_test, numeric_test, char_test, varchar_test, tinytext_test, text_test, mediumtext_test, longtext_test, binary_test, varbinary_test, tinyblob_test, blob_test, mediumblob_test, longblob_test, bit_test, date_test, datetime_test, datetime6_test, timestamp_test, time_test, json_test, mood, tag FROM test_inner_mysql_types WHERE table_id = %s
"""

GET_MANY_MYSQL_TYPE: typing.Final[str] = """-- name: GetManyMysqlType :many
SELECT id, int_test, integer_test, mediumint_test, smallint_test, tinyint_test, bigint_test, int_unsigned_test, bigint_unsigned_test, year_test, tinyint1_test, bool_test, boolean_test, float_test, double_test, double_precision_test, real_test, decimal_test, numeric_test, char_test, varchar_test, tinytext_test, text_test, mediumtext_test, longtext_test, binary_test, varbinary_test, tinyblob_test, blob_test, mediumblob_test, longblob_test, bit_test, date_test, datetime_test, datetime6_test, timestamp_test, time_test, json_test, mood, tag FROM test_mysql_types WHERE id = %s
"""

GET_MANY_INNER_MYSQL_TYPE: typing.Final[str] = """-- name: GetManyInnerMysqlType :many
SELECT table_id, int_test, integer_test, mediumint_test, smallint_test, tinyint_test, bigint_test, int_unsigned_test, bigint_unsigned_test, year_test, tinyint1_test, bool_test, boolean_test, float_test, double_test, double_precision_test, real_test, decimal_test, numeric_test, char_test, varchar_test, tinytext_test, text_test, mediumtext_test, longtext_test, binary_test, varbinary_test, tinyblob_test, blob_test, mediumblob_test, longblob_test, bit_test, date_test, datetime_test, datetime6_test, timestamp_test, time_test, json_test, mood, tag FROM test_inner_mysql_types WHERE table_id = %s
"""

GET_MANY_NULLABLE_INNER_MYSQL_TYPE: typing.Final[str] = """-- name: GetManyNullableInnerMysqlType :many
SELECT table_id, int_test, integer_test, mediumint_test, smallint_test, tinyint_test, bigint_test, int_unsigned_test, bigint_unsigned_test, year_test, tinyint1_test, bool_test, boolean_test, float_test, double_test, double_precision_test, real_test, decimal_test, numeric_test, char_test, varchar_test, tinytext_test, text_test, mediumtext_test, longtext_test, binary_test, varbinary_test, tinyblob_test, blob_test, mediumblob_test, longblob_test, bit_test, date_test, datetime_test, datetime6_test, timestamp_test, time_test, json_test, mood, tag FROM test_inner_mysql_types WHERE table_id = %s AND int_test <=> %s
"""

GET_ONE_DATE: typing.Final[str] = """-- name: GetOneDate :one
SELECT date_test FROM test_mysql_types WHERE id = %s AND date_test = %s
"""

GET_ONE_DATETIME: typing.Final[str] = """-- name: GetOneDatetime :one
SELECT datetime_test FROM test_mysql_types WHERE id = %s AND datetime_test = %s
"""

GET_ONE_TIME: typing.Final[str] = """-- name: GetOneTime :one
SELECT time_test FROM test_mysql_types WHERE id = %s AND time_test = %s
"""

GET_ONE_BOOL: typing.Final[str] = """-- name: GetOneBool :one
SELECT tinyint1_test FROM test_mysql_types WHERE id = %s AND tinyint1_test = %s
"""

GET_ONE_DECIMAL: typing.Final[str] = """-- name: GetOneDecimal :one
SELECT decimal_test FROM test_mysql_types WHERE id = %s AND decimal_test = %s
"""

GET_ONE_BLOB: typing.Final[str] = """-- name: GetOneBlob :one
SELECT blob_test FROM test_mysql_types WHERE id = %s AND blob_test = %s
"""

GET_ONE_BIT: typing.Final[str] = """-- name: GetOneBit :one
SELECT bit_test FROM test_mysql_types WHERE id = %s
"""

GET_ONE_YEAR: typing.Final[str] = """-- name: GetOneYear :one
SELECT year_test FROM test_mysql_types WHERE id = %s
"""

GET_ONE_JSON: typing.Final[str] = """-- name: GetOneJson :one
SELECT json_test FROM test_mysql_types WHERE id = %s
"""

GET_ONE_MOOD: typing.Final[str] = """-- name: GetOneMood :one
SELECT mood FROM test_mysql_types WHERE id = %s AND mood = %s
"""

GET_ONE_TAG: typing.Final[str] = """-- name: GetOneTag :one
SELECT tag FROM test_mysql_types WHERE id = %s
"""

GET_MANY_DATE: typing.Final[str] = """-- name: GetManyDate :many
SELECT date_test FROM test_mysql_types WHERE id = %s AND date_test = %s
"""

GET_MANY_TIME: typing.Final[str] = """-- name: GetManyTime :many
SELECT time_test FROM test_mysql_types WHERE id = %s AND time_test = %s
"""

GET_MANY_BOOL: typing.Final[str] = """-- name: GetManyBool :many
SELECT tinyint1_test FROM test_mysql_types WHERE id = %s AND tinyint1_test = %s
"""

GET_MANY_DECIMAL: typing.Final[str] = """-- name: GetManyDecimal :many
SELECT decimal_test FROM test_mysql_types WHERE id = %s AND decimal_test = %s
"""

GET_MANY_MOOD: typing.Final[str] = """-- name: GetManyMood :many
SELECT mood FROM test_mysql_types WHERE mood = %s ORDER BY id
"""

LIST_MONTHS: typing.Final[str] = """-- name: ListMonths :many
SELECT DATE_FORMAT(datetime_test, '%%Y-%%m') AS month FROM test_mysql_types ORDER BY id
"""

COUNT_MYSQL_TYPES: typing.Final[str] = """-- name: CountMysqlTypes :one
SELECT count(*) FROM test_mysql_types
"""

UPDATE_VARCHAR_TEST: typing.Final[str] = """-- name: UpdateVarcharTest :execrows
UPDATE test_mysql_types SET varchar_test = %s WHERE id = %s
"""

DELETE_ONE_MYSQL_TYPE: typing.Final[str] = """-- name: DeleteOneMysqlType :exec
DELETE FROM test_mysql_types WHERE id = %s
"""

ALL_MYSQL_TYPES_CURSOR: typing.Final[str] = """-- name: AllMysqlTypesCursor :execresult
SELECT id, int_test, integer_test, mediumint_test, smallint_test, tinyint_test, bigint_test, int_unsigned_test, bigint_unsigned_test, year_test, tinyint1_test, bool_test, boolean_test, float_test, double_test, double_precision_test, real_test, decimal_test, numeric_test, char_test, varchar_test, tinytext_test, text_test, mediumtext_test, longtext_test, binary_test, varbinary_test, tinyblob_test, blob_test, mediumblob_test, longblob_test, bit_test, date_test, datetime_test, datetime6_test, timestamp_test, time_test, json_test, mood, tag FROM test_mysql_types
"""

INSERT_EXEC_LAST_ID: typing.Final[str] = """-- name: InsertExecLastId :execlastid
INSERT INTO test_execlastid (name) VALUES (%s)
"""

GET_EXEC_LAST_ID_NAME: typing.Final[str] = """-- name: GetExecLastIdName :one
SELECT name FROM test_execlastid WHERE id = %s
"""

INSERT_TYPE_OVERRIDE: typing.Final[str] = """-- name: InsertTypeOverride :exec
INSERT INTO test_type_override (id, text_test) VALUES (%s, %s)
"""

GET_TYPE_OVERRIDE: typing.Final[str] = """-- name: GetTypeOverride :one
SELECT id, text_test FROM test_type_override WHERE id = %s
"""

GET_RESERVED_ARG: typing.Final[str] = """-- name: GetReservedArg :one
SELECT id, conn FROM test_reserved_args WHERE conn = %s
"""

INSERT_RESERVED_ARG: typing.Final[str] = """-- name: InsertReservedArg :exec
INSERT INTO test_reserved_args (id, conn) VALUES (%s, %s)
"""

TOUCH_EXEC_LAST_ID: typing.Final[str] = """-- name: TouchExecLastId :execlastid
UPDATE test_execlastid SET name = %s WHERE id = %s
"""


class QueryResults[T]:
    """Helper class that allows both iteration and normal fetching of data from the db."""

    __slots__ = ("_args", "_conn", "_cursor", "_decode_hook", "_sql")

    def __init__(
        self,
        conn: asyncmy.Connection,
        sql: str,
        decode_hook: collections.abc.Callable[[tuple[typing.Any, ...]], T],
        *args: QueryResultsArgsType,
    ) -> None:
        """Initialize the QueryResults instance.

        Args:
            conn:
                The connection object of type `asyncmy.Connection` used to execute queries.
            sql:
                The SQL statement that will be executed when fetching/iterating.
            decode_hook:
                A callback that turns an `tuple[typing.Any, ...]` object into `T` that will be returned.
            *args:
                Arguments that should be sent when executing the sql query.
        """
        self._conn = conn
        self._sql = sql
        self._decode_hook = decode_hook
        self._args = args
        self._cursor: asyncmy.cursors.Cursor | None = None

    def __aiter__(self) -> QueryResults[T]:
        """Initialize iteration support for `async for`.

        Returns:
            Self as an asynchronous iterator.
        """
        return self

    def __await__(
        self,
    ) -> collections.abc.Generator[None, None, collections.abc.Sequence[T]]:
        """Allow `await` on the object to return all rows as a fully decoded sequence.

        Returns:
            A sequence of decoded objects of type `T`.
        """

        async def _wrapper() -> collections.abc.Sequence[T]:
            cur = self._conn.cursor()
            await cur.execute(self._sql, self._args)
            result = await cur.fetchall()
            await cur.close()
            return [self._decode_hook(row) for row in result]

        return _wrapper().__await__()

    async def __anext__(self) -> T:
        """Yield the next item in the query result using an asyncmy cursor.

        Returns:
            The next decoded result of type `T`.

        Raises:
            StopAsyncIteration: When no more records are available.
        """
        if self._cursor is None:
            self._cursor = self._conn.cursor()
            await self._cursor.execute(self._sql, self._args)
        record = await self._cursor.fetchone()
        if record is None:
            self._cursor = None
            raise StopAsyncIteration
        return self._decode_hook(record)


async def insert_one_mysql_type(
    conn: asyncmy.Connection,
    *,
    id_: int,
    int_test: int,
    integer_test: int,
    mediumint_test: int,
    smallint_test: int,
    tinyint_test: int,
    bigint_test: int,
    int_unsigned_test: int,
    bigint_unsigned_test: int,
    year_test: int,
    tinyint1_test: bool,
    bool_test: bool,
    boolean_test: bool,
    float_test: float,
    double_test: float,
    double_precision_test: float,
    real_test: float,
    decimal_test: decimal.Decimal,
    numeric_test: decimal.Decimal,
    char_test: str,
    varchar_test: str,
    tinytext_test: str,
    text_test: str,
    mediumtext_test: str,
    longtext_test: str,
    binary_test: memoryview,
    varbinary_test: memoryview,
    tinyblob_test: memoryview,
    blob_test: memoryview,
    mediumblob_test: memoryview,
    longblob_test: memoryview,
    bit_test: memoryview,
    date_test: datetime.date,
    datetime_test: datetime.datetime,
    datetime6_test: datetime.datetime,
    timestamp_test: datetime.datetime,
    time_test: datetime.timedelta,
    json_test: str,
    mood: enums.TestMysqlTypesMood,
    tag: enums.TestMysqlTypesTag,
) -> None:
    """Execute SQL query with `name: InsertOneMysqlType :exec`.

    ```sql
    INSERT INTO test_mysql_types (
        id, int_test, integer_test, mediumint_test, smallint_test, tinyint_test, bigint_test,
        int_unsigned_test, bigint_unsigned_test, year_test,
        tinyint1_test, bool_test, boolean_test,
        float_test, double_test, double_precision_test, real_test,
        decimal_test, numeric_test,
        char_test, varchar_test, tinytext_test, text_test, mediumtext_test, longtext_test,
        binary_test, varbinary_test, tinyblob_test, blob_test, mediumblob_test, longblob_test, bit_test,
        date_test, datetime_test, datetime6_test, timestamp_test, time_test,
        json_test, mood, tag
    ) VALUES (
                 %s, %s, %s, %s, %s, %s, %s,
                 %s, %s, %s,
                 %s, %s, %s,
                 %s, %s, %s, %s,
                 %s, %s,
                 %s, %s, %s, %s, %s, %s,
                 %s, %s, %s, %s, %s, %s, %s,
                 %s, %s, %s, %s, %s,
                 %s, %s, %s
             )
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.
        int_test: int.
        integer_test: int.
        mediumint_test: int.
        smallint_test: int.
        tinyint_test: int.
        bigint_test: int.
        int_unsigned_test: int.
        bigint_unsigned_test: int.
        year_test: int.
        tinyint1_test: bool.
        bool_test: bool.
        boolean_test: bool.
        float_test: float.
        double_test: float.
        double_precision_test: float.
        real_test: float.
        decimal_test: decimal.Decimal.
        numeric_test: decimal.Decimal.
        char_test: str.
        varchar_test: str.
        tinytext_test: str.
        text_test: str.
        mediumtext_test: str.
        longtext_test: str.
        binary_test: memoryview.
        varbinary_test: memoryview.
        tinyblob_test: memoryview.
        blob_test: memoryview.
        mediumblob_test: memoryview.
        longblob_test: memoryview.
        bit_test: memoryview.
        date_test: datetime.date.
        datetime_test: datetime.datetime.
        datetime6_test: datetime.datetime.
        timestamp_test: datetime.datetime.
        time_test: datetime.timedelta.
        json_test: str.
        mood: enums.TestMysqlTypesMood.
        tag: enums.TestMysqlTypesTag.
    """
    async with conn.cursor() as cur:
        sql_args = (
            id_,
            int_test,
            integer_test,
            mediumint_test,
            smallint_test,
            tinyint_test,
            bigint_test,
            int_unsigned_test,
            bigint_unsigned_test,
            year_test,
            tinyint1_test,
            bool_test,
            boolean_test,
            float_test,
            double_test,
            double_precision_test,
            real_test,
            decimal_test,
            numeric_test,
            char_test,
            varchar_test,
            tinytext_test,
            text_test,
            mediumtext_test,
            longtext_test,
            bytes(binary_test),
            bytes(varbinary_test),
            bytes(tinyblob_test),
            bytes(blob_test),
            bytes(mediumblob_test),
            bytes(longblob_test),
            bytes(bit_test),
            date_test,
            datetime_test,
            datetime6_test,
            timestamp_test,
            time_test,
            json_test,
            mood,
            tag,
        )
        await cur.execute(INSERT_ONE_MYSQL_TYPE, sql_args)


async def insert_one_inner_mysql_type(
    conn: asyncmy.Connection,
    *,
    table_id: int,
    int_test: int | None,
    integer_test: int | None,
    mediumint_test: int | None,
    smallint_test: int | None,
    tinyint_test: int | None,
    bigint_test: int | None,
    int_unsigned_test: int | None,
    bigint_unsigned_test: int | None,
    year_test: int | None,
    tinyint1_test: bool | None,
    bool_test: bool | None,
    boolean_test: bool | None,
    float_test: float | None,
    double_test: float | None,
    double_precision_test: float | None,
    real_test: float | None,
    decimal_test: decimal.Decimal | None,
    numeric_test: decimal.Decimal | None,
    char_test: str | None,
    varchar_test: str | None,
    tinytext_test: str | None,
    text_test: str | None,
    mediumtext_test: str | None,
    longtext_test: str | None,
    binary_test: memoryview | None,
    varbinary_test: memoryview | None,
    tinyblob_test: memoryview | None,
    blob_test: memoryview | None,
    mediumblob_test: memoryview | None,
    longblob_test: memoryview | None,
    bit_test: memoryview | None,
    date_test: datetime.date | None,
    datetime_test: datetime.datetime | None,
    datetime6_test: datetime.datetime | None,
    timestamp_test: datetime.datetime | None,
    time_test: datetime.timedelta | None,
    json_test: str | None,
    mood: enums.TestInnerMysqlTypesMood | None,
    tag: enums.TestInnerMysqlTypesTag | None,
) -> None:
    """Execute SQL query with `name: InsertOneInnerMysqlType :exec`.

    ```sql
    INSERT INTO test_inner_mysql_types (
        table_id, int_test, integer_test, mediumint_test, smallint_test, tinyint_test, bigint_test,
        int_unsigned_test, bigint_unsigned_test, year_test,
        tinyint1_test, bool_test, boolean_test,
        float_test, double_test, double_precision_test, real_test,
        decimal_test, numeric_test,
        char_test, varchar_test, tinytext_test, text_test, mediumtext_test, longtext_test,
        binary_test, varbinary_test, tinyblob_test, blob_test, mediumblob_test, longblob_test, bit_test,
        date_test, datetime_test, datetime6_test, timestamp_test, time_test,
        json_test, mood, tag
    ) VALUES (
                 %s, %s, %s, %s, %s, %s, %s,
                 %s, %s, %s,
                 %s, %s, %s,
                 %s, %s, %s, %s,
                 %s, %s,
                 %s, %s, %s, %s, %s, %s,
                 %s, %s, %s, %s, %s, %s, %s,
                 %s, %s, %s, %s, %s,
                 %s, %s, %s
             )
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        table_id: int.
        int_test: int | None.
        integer_test: int | None.
        mediumint_test: int | None.
        smallint_test: int | None.
        tinyint_test: int | None.
        bigint_test: int | None.
        int_unsigned_test: int | None.
        bigint_unsigned_test: int | None.
        year_test: int | None.
        tinyint1_test: bool | None.
        bool_test: bool | None.
        boolean_test: bool | None.
        float_test: float | None.
        double_test: float | None.
        double_precision_test: float | None.
        real_test: float | None.
        decimal_test: decimal.Decimal | None.
        numeric_test: decimal.Decimal | None.
        char_test: str | None.
        varchar_test: str | None.
        tinytext_test: str | None.
        text_test: str | None.
        mediumtext_test: str | None.
        longtext_test: str | None.
        binary_test: memoryview | None.
        varbinary_test: memoryview | None.
        tinyblob_test: memoryview | None.
        blob_test: memoryview | None.
        mediumblob_test: memoryview | None.
        longblob_test: memoryview | None.
        bit_test: memoryview | None.
        date_test: datetime.date | None.
        datetime_test: datetime.datetime | None.
        datetime6_test: datetime.datetime | None.
        timestamp_test: datetime.datetime | None.
        time_test: datetime.timedelta | None.
        json_test: str | None.
        mood: enums.TestInnerMysqlTypesMood | None.
        tag: enums.TestInnerMysqlTypesTag | None.
    """
    async with conn.cursor() as cur:
        sql_args = (
            table_id,
            int_test,
            integer_test,
            mediumint_test,
            smallint_test,
            tinyint_test,
            bigint_test,
            int_unsigned_test,
            bigint_unsigned_test,
            year_test,
            tinyint1_test,
            bool_test,
            boolean_test,
            float_test,
            double_test,
            double_precision_test,
            real_test,
            decimal_test,
            numeric_test,
            char_test,
            varchar_test,
            tinytext_test,
            text_test,
            mediumtext_test,
            longtext_test,
            bytes(binary_test) if binary_test is not None else None,
            bytes(varbinary_test) if varbinary_test is not None else None,
            bytes(tinyblob_test) if tinyblob_test is not None else None,
            bytes(blob_test) if blob_test is not None else None,
            bytes(mediumblob_test) if mediumblob_test is not None else None,
            bytes(longblob_test) if longblob_test is not None else None,
            bytes(bit_test) if bit_test is not None else None,
            date_test,
            datetime_test,
            datetime6_test,
            timestamp_test,
            time_test,
            json_test,
            mood,
            tag,
        )
        await cur.execute(INSERT_ONE_INNER_MYSQL_TYPE, sql_args)


async def get_one_mysql_type(conn: asyncmy.Connection, *, id_: int) -> models.TestMysqlType | None:
    """Fetch one from the db using the SQL query with `name: GetOneMysqlType :one`.

    ```sql
    SELECT id, int_test, integer_test, mediumint_test, smallint_test, tinyint_test, bigint_test, int_unsigned_test, bigint_unsigned_test, year_test, tinyint1_test, bool_test, boolean_test, float_test, double_test, double_precision_test, real_test, decimal_test, numeric_test, char_test, varchar_test, tinytext_test, text_test, mediumtext_test, longtext_test, binary_test, varbinary_test, tinyblob_test, blob_test, mediumblob_test, longblob_test, bit_test, date_test, datetime_test, datetime6_test, timestamp_test, time_test, json_test, mood, tag FROM test_mysql_types WHERE id = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.

    Returns:
        Result of type `models.TestMysqlType` fetched from the db. Will be `None` if not found.
    """
    async with conn.cursor() as cur:
        await cur.execute(GET_ONE_MYSQL_TYPE, (id_,))
        row = await cur.fetchone()
    if row is None:
        return None
    return models.TestMysqlType(
        id_=row[0],
        int_test=row[1],
        integer_test=row[2],
        mediumint_test=row[3],
        smallint_test=row[4],
        tinyint_test=int(row[5]),
        bigint_test=row[6],
        int_unsigned_test=row[7],
        bigint_unsigned_test=row[8],
        year_test=row[9],
        tinyint1_test=bool(row[10]),
        bool_test=bool(row[11]),
        boolean_test=bool(row[12]),
        float_test=row[13],
        double_test=row[14],
        double_precision_test=row[15],
        real_test=row[16],
        decimal_test=row[17],
        numeric_test=row[18],
        char_test=row[19],
        varchar_test=row[20],
        tinytext_test=row[21],
        text_test=row[22],
        mediumtext_test=row[23],
        longtext_test=row[24],
        binary_test=memoryview(row[25]),
        varbinary_test=memoryview(row[26]),
        tinyblob_test=memoryview(row[27]),
        blob_test=memoryview(row[28]),
        mediumblob_test=memoryview(row[29]),
        longblob_test=memoryview(row[30]),
        bit_test=memoryview(row[31]),
        date_test=row[32],
        datetime_test=row[33],
        datetime6_test=row[34],
        timestamp_test=row[35],
        time_test=row[36],
        json_test=row[37],
        mood=enums.TestMysqlTypesMood(row[38]),
        tag=enums.TestMysqlTypesTag(row[39]),
    )


async def get_one_inner_mysql_type(conn: asyncmy.Connection, *, table_id: int) -> models.TestInnerMysqlType | None:
    """Fetch one from the db using the SQL query with `name: GetOneInnerMysqlType :one`.

    ```sql
    SELECT table_id, int_test, integer_test, mediumint_test, smallint_test, tinyint_test, bigint_test, int_unsigned_test, bigint_unsigned_test, year_test, tinyint1_test, bool_test, boolean_test, float_test, double_test, double_precision_test, real_test, decimal_test, numeric_test, char_test, varchar_test, tinytext_test, text_test, mediumtext_test, longtext_test, binary_test, varbinary_test, tinyblob_test, blob_test, mediumblob_test, longblob_test, bit_test, date_test, datetime_test, datetime6_test, timestamp_test, time_test, json_test, mood, tag FROM test_inner_mysql_types WHERE table_id = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        table_id: int.

    Returns:
        Result of type `models.TestInnerMysqlType` fetched from the db. Will be `None` if not found.
    """
    async with conn.cursor() as cur:
        await cur.execute(GET_ONE_INNER_MYSQL_TYPE, (table_id,))
        row = await cur.fetchone()
    if row is None:
        return None
    return models.TestInnerMysqlType(
        table_id=row[0],
        int_test=row[1],
        integer_test=row[2],
        mediumint_test=row[3],
        smallint_test=row[4],
        tinyint_test=int(row[5]) if row[5] is not None else None,
        bigint_test=row[6],
        int_unsigned_test=row[7],
        bigint_unsigned_test=row[8],
        year_test=row[9],
        tinyint1_test=bool(row[10]) if row[10] is not None else None,
        bool_test=bool(row[11]) if row[11] is not None else None,
        boolean_test=bool(row[12]) if row[12] is not None else None,
        float_test=row[13],
        double_test=row[14],
        double_precision_test=row[15],
        real_test=row[16],
        decimal_test=row[17],
        numeric_test=row[18],
        char_test=row[19],
        varchar_test=row[20],
        tinytext_test=row[21],
        text_test=row[22],
        mediumtext_test=row[23],
        longtext_test=row[24],
        binary_test=memoryview(row[25]) if row[25] is not None else None,
        varbinary_test=memoryview(row[26]) if row[26] is not None else None,
        tinyblob_test=memoryview(row[27]) if row[27] is not None else None,
        blob_test=memoryview(row[28]) if row[28] is not None else None,
        mediumblob_test=memoryview(row[29]) if row[29] is not None else None,
        longblob_test=memoryview(row[30]) if row[30] is not None else None,
        bit_test=memoryview(row[31]) if row[31] is not None else None,
        date_test=row[32],
        datetime_test=row[33],
        datetime6_test=row[34],
        timestamp_test=row[35],
        time_test=row[36],
        json_test=row[37],
        mood=enums.TestInnerMysqlTypesMood(row[38]) if row[38] is not None else None,
        tag=enums.TestInnerMysqlTypesTag(row[39]) if row[39] is not None else None,
    )


def get_many_mysql_type(conn: asyncmy.Connection, *, id_: int) -> QueryResults[models.TestMysqlType]:
    """Fetch many from the db using the SQL query with `name: GetManyMysqlType :many`.

    ```sql
    SELECT id, int_test, integer_test, mediumint_test, smallint_test, tinyint_test, bigint_test, int_unsigned_test, bigint_unsigned_test, year_test, tinyint1_test, bool_test, boolean_test, float_test, double_test, double_precision_test, real_test, decimal_test, numeric_test, char_test, varchar_test, tinytext_test, text_test, mediumtext_test, longtext_test, binary_test, varbinary_test, tinyblob_test, blob_test, mediumblob_test, longblob_test, bit_test, date_test, datetime_test, datetime6_test, timestamp_test, time_test, json_test, mood, tag FROM test_mysql_types WHERE id = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.

    Returns:
        Helper class of type `QueryResults[models.TestMysqlType]` that allows both iteration and normal fetching of data from the db.
    """

    def _decode_hook(row: tuple[typing.Any, ...]) -> models.TestMysqlType:
        return models.TestMysqlType(
            id_=row[0],
            int_test=row[1],
            integer_test=row[2],
            mediumint_test=row[3],
            smallint_test=row[4],
            tinyint_test=int(row[5]),
            bigint_test=row[6],
            int_unsigned_test=row[7],
            bigint_unsigned_test=row[8],
            year_test=row[9],
            tinyint1_test=bool(row[10]),
            bool_test=bool(row[11]),
            boolean_test=bool(row[12]),
            float_test=row[13],
            double_test=row[14],
            double_precision_test=row[15],
            real_test=row[16],
            decimal_test=row[17],
            numeric_test=row[18],
            char_test=row[19],
            varchar_test=row[20],
            tinytext_test=row[21],
            text_test=row[22],
            mediumtext_test=row[23],
            longtext_test=row[24],
            binary_test=memoryview(row[25]),
            varbinary_test=memoryview(row[26]),
            tinyblob_test=memoryview(row[27]),
            blob_test=memoryview(row[28]),
            mediumblob_test=memoryview(row[29]),
            longblob_test=memoryview(row[30]),
            bit_test=memoryview(row[31]),
            date_test=row[32],
            datetime_test=row[33],
            datetime6_test=row[34],
            timestamp_test=row[35],
            time_test=row[36],
            json_test=row[37],
            mood=enums.TestMysqlTypesMood(row[38]),
            tag=enums.TestMysqlTypesTag(row[39]),
        )

    return QueryResults(conn, GET_MANY_MYSQL_TYPE, _decode_hook, id_)


def get_many_inner_mysql_type(conn: asyncmy.Connection, *, table_id: int) -> QueryResults[models.TestInnerMysqlType]:
    """Fetch many from the db using the SQL query with `name: GetManyInnerMysqlType :many`.

    ```sql
    SELECT table_id, int_test, integer_test, mediumint_test, smallint_test, tinyint_test, bigint_test, int_unsigned_test, bigint_unsigned_test, year_test, tinyint1_test, bool_test, boolean_test, float_test, double_test, double_precision_test, real_test, decimal_test, numeric_test, char_test, varchar_test, tinytext_test, text_test, mediumtext_test, longtext_test, binary_test, varbinary_test, tinyblob_test, blob_test, mediumblob_test, longblob_test, bit_test, date_test, datetime_test, datetime6_test, timestamp_test, time_test, json_test, mood, tag FROM test_inner_mysql_types WHERE table_id = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        table_id: int.

    Returns:
        Helper class of type `QueryResults[models.TestInnerMysqlType]` that allows both iteration and normal fetching of data from the db.
    """

    def _decode_hook(row: tuple[typing.Any, ...]) -> models.TestInnerMysqlType:
        return models.TestInnerMysqlType(
            table_id=row[0],
            int_test=row[1],
            integer_test=row[2],
            mediumint_test=row[3],
            smallint_test=row[4],
            tinyint_test=int(row[5]) if row[5] is not None else None,
            bigint_test=row[6],
            int_unsigned_test=row[7],
            bigint_unsigned_test=row[8],
            year_test=row[9],
            tinyint1_test=bool(row[10]) if row[10] is not None else None,
            bool_test=bool(row[11]) if row[11] is not None else None,
            boolean_test=bool(row[12]) if row[12] is not None else None,
            float_test=row[13],
            double_test=row[14],
            double_precision_test=row[15],
            real_test=row[16],
            decimal_test=row[17],
            numeric_test=row[18],
            char_test=row[19],
            varchar_test=row[20],
            tinytext_test=row[21],
            text_test=row[22],
            mediumtext_test=row[23],
            longtext_test=row[24],
            binary_test=memoryview(row[25]) if row[25] is not None else None,
            varbinary_test=memoryview(row[26]) if row[26] is not None else None,
            tinyblob_test=memoryview(row[27]) if row[27] is not None else None,
            blob_test=memoryview(row[28]) if row[28] is not None else None,
            mediumblob_test=memoryview(row[29]) if row[29] is not None else None,
            longblob_test=memoryview(row[30]) if row[30] is not None else None,
            bit_test=memoryview(row[31]) if row[31] is not None else None,
            date_test=row[32],
            datetime_test=row[33],
            datetime6_test=row[34],
            timestamp_test=row[35],
            time_test=row[36],
            json_test=row[37],
            mood=enums.TestInnerMysqlTypesMood(row[38]) if row[38] is not None else None,
            tag=enums.TestInnerMysqlTypesTag(row[39]) if row[39] is not None else None,
        )

    return QueryResults(conn, GET_MANY_INNER_MYSQL_TYPE, _decode_hook, table_id)


def get_many_nullable_inner_mysql_type(conn: asyncmy.Connection, *, table_id: int, int_test: int | None) -> QueryResults[models.TestInnerMysqlType]:
    """Fetch many from the db using the SQL query with `name: GetManyNullableInnerMysqlType :many`.

    ```sql
    SELECT table_id, int_test, integer_test, mediumint_test, smallint_test, tinyint_test, bigint_test, int_unsigned_test, bigint_unsigned_test, year_test, tinyint1_test, bool_test, boolean_test, float_test, double_test, double_precision_test, real_test, decimal_test, numeric_test, char_test, varchar_test, tinytext_test, text_test, mediumtext_test, longtext_test, binary_test, varbinary_test, tinyblob_test, blob_test, mediumblob_test, longblob_test, bit_test, date_test, datetime_test, datetime6_test, timestamp_test, time_test, json_test, mood, tag FROM test_inner_mysql_types WHERE table_id = %s AND int_test <=> %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        table_id: int.
        int_test: int | None.

    Returns:
        Helper class of type `QueryResults[models.TestInnerMysqlType]` that allows both iteration and normal fetching of data from the db.
    """

    def _decode_hook(row: tuple[typing.Any, ...]) -> models.TestInnerMysqlType:
        return models.TestInnerMysqlType(
            table_id=row[0],
            int_test=row[1],
            integer_test=row[2],
            mediumint_test=row[3],
            smallint_test=row[4],
            tinyint_test=int(row[5]) if row[5] is not None else None,
            bigint_test=row[6],
            int_unsigned_test=row[7],
            bigint_unsigned_test=row[8],
            year_test=row[9],
            tinyint1_test=bool(row[10]) if row[10] is not None else None,
            bool_test=bool(row[11]) if row[11] is not None else None,
            boolean_test=bool(row[12]) if row[12] is not None else None,
            float_test=row[13],
            double_test=row[14],
            double_precision_test=row[15],
            real_test=row[16],
            decimal_test=row[17],
            numeric_test=row[18],
            char_test=row[19],
            varchar_test=row[20],
            tinytext_test=row[21],
            text_test=row[22],
            mediumtext_test=row[23],
            longtext_test=row[24],
            binary_test=memoryview(row[25]) if row[25] is not None else None,
            varbinary_test=memoryview(row[26]) if row[26] is not None else None,
            tinyblob_test=memoryview(row[27]) if row[27] is not None else None,
            blob_test=memoryview(row[28]) if row[28] is not None else None,
            mediumblob_test=memoryview(row[29]) if row[29] is not None else None,
            longblob_test=memoryview(row[30]) if row[30] is not None else None,
            bit_test=memoryview(row[31]) if row[31] is not None else None,
            date_test=row[32],
            datetime_test=row[33],
            datetime6_test=row[34],
            timestamp_test=row[35],
            time_test=row[36],
            json_test=row[37],
            mood=enums.TestInnerMysqlTypesMood(row[38]) if row[38] is not None else None,
            tag=enums.TestInnerMysqlTypesTag(row[39]) if row[39] is not None else None,
        )

    return QueryResults(conn, GET_MANY_NULLABLE_INNER_MYSQL_TYPE, _decode_hook, table_id, int_test)


async def get_one_date(conn: asyncmy.Connection, *, id_: int, date_test: datetime.date) -> datetime.date | None:
    """Fetch one from the db using the SQL query with `name: GetOneDate :one`.

    ```sql
    SELECT date_test FROM test_mysql_types WHERE id = %s AND date_test = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.
        date_test: datetime.date.

    Returns:
        Result of type `datetime.date` fetched from the db. Will be `None` if not found.
    """
    async with conn.cursor() as cur:
        await cur.execute(GET_ONE_DATE, (id_, date_test))
        row = await cur.fetchone()
    if row is None:
        return None
    return row[0]


async def get_one_datetime(conn: asyncmy.Connection, *, id_: int, datetime_test: datetime.datetime) -> datetime.datetime | None:
    """Fetch one from the db using the SQL query with `name: GetOneDatetime :one`.

    ```sql
    SELECT datetime_test FROM test_mysql_types WHERE id = %s AND datetime_test = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.
        datetime_test: datetime.datetime.

    Returns:
        Result of type `datetime.datetime` fetched from the db. Will be `None` if not found.
    """
    async with conn.cursor() as cur:
        await cur.execute(GET_ONE_DATETIME, (id_, datetime_test))
        row = await cur.fetchone()
    if row is None:
        return None
    return row[0]


async def get_one_time(conn: asyncmy.Connection, *, id_: int, time_test: datetime.timedelta) -> datetime.timedelta | None:
    """Fetch one from the db using the SQL query with `name: GetOneTime :one`.

    ```sql
    SELECT time_test FROM test_mysql_types WHERE id = %s AND time_test = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.
        time_test: datetime.timedelta.

    Returns:
        Result of type `datetime.timedelta` fetched from the db. Will be `None` if not found.
    """
    async with conn.cursor() as cur:
        await cur.execute(GET_ONE_TIME, (id_, time_test))
        row = await cur.fetchone()
    if row is None:
        return None
    return row[0]


async def get_one_bool(conn: asyncmy.Connection, *, id_: int, tinyint1_test: bool) -> bool | None:
    """Fetch one from the db using the SQL query with `name: GetOneBool :one`.

    ```sql
    SELECT tinyint1_test FROM test_mysql_types WHERE id = %s AND tinyint1_test = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.
        tinyint1_test: bool.

    Returns:
        Result of type `bool` fetched from the db. Will be `None` if not found.
    """
    async with conn.cursor() as cur:
        await cur.execute(GET_ONE_BOOL, (id_, tinyint1_test))
        row = await cur.fetchone()
    if row is None:
        return None
    return bool(row[0])


async def get_one_decimal(conn: asyncmy.Connection, *, id_: int, decimal_test: decimal.Decimal) -> decimal.Decimal | None:
    """Fetch one from the db using the SQL query with `name: GetOneDecimal :one`.

    ```sql
    SELECT decimal_test FROM test_mysql_types WHERE id = %s AND decimal_test = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.
        decimal_test: decimal.Decimal.

    Returns:
        Result of type `decimal.Decimal` fetched from the db. Will be `None` if not found.
    """
    async with conn.cursor() as cur:
        await cur.execute(GET_ONE_DECIMAL, (id_, decimal_test))
        row = await cur.fetchone()
    if row is None:
        return None
    return row[0]


async def get_one_blob(conn: asyncmy.Connection, *, id_: int, blob_test: memoryview) -> memoryview | None:
    """Fetch one from the db using the SQL query with `name: GetOneBlob :one`.

    ```sql
    SELECT blob_test FROM test_mysql_types WHERE id = %s AND blob_test = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.
        blob_test: memoryview.

    Returns:
        Result of type `memoryview` fetched from the db. Will be `None` if not found.
    """
    async with conn.cursor() as cur:
        await cur.execute(GET_ONE_BLOB, (id_, bytes(blob_test)))
        row = await cur.fetchone()
    if row is None:
        return None
    return memoryview(row[0])


async def get_one_bit(conn: asyncmy.Connection, *, id_: int) -> memoryview | None:
    """Fetch one from the db using the SQL query with `name: GetOneBit :one`.

    ```sql
    SELECT bit_test FROM test_mysql_types WHERE id = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.

    Returns:
        Result of type `memoryview` fetched from the db. Will be `None` if not found.
    """
    async with conn.cursor() as cur:
        await cur.execute(GET_ONE_BIT, (id_,))
        row = await cur.fetchone()
    if row is None:
        return None
    return memoryview(row[0])


async def get_one_year(conn: asyncmy.Connection, *, id_: int) -> int | None:
    """Fetch one from the db using the SQL query with `name: GetOneYear :one`.

    ```sql
    SELECT year_test FROM test_mysql_types WHERE id = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.

    Returns:
        Result of type `int` fetched from the db. Will be `None` if not found.
    """
    async with conn.cursor() as cur:
        await cur.execute(GET_ONE_YEAR, (id_,))
        row = await cur.fetchone()
    if row is None:
        return None
    return row[0]


async def get_one_json(conn: asyncmy.Connection, *, id_: int) -> str | None:
    """Fetch one from the db using the SQL query with `name: GetOneJson :one`.

    ```sql
    SELECT json_test FROM test_mysql_types WHERE id = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.

    Returns:
        Result of type `str` fetched from the db. Will be `None` if not found.
    """
    async with conn.cursor() as cur:
        await cur.execute(GET_ONE_JSON, (id_,))
        row = await cur.fetchone()
    if row is None:
        return None
    return row[0]


async def get_one_mood(conn: asyncmy.Connection, *, id_: int, mood: enums.TestMysqlTypesMood) -> enums.TestMysqlTypesMood | None:
    """Fetch one from the db using the SQL query with `name: GetOneMood :one`.

    ```sql
    SELECT mood FROM test_mysql_types WHERE id = %s AND mood = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.
        mood: enums.TestMysqlTypesMood.

    Returns:
        Result of type `enums.TestMysqlTypesMood` fetched from the db. Will be `None` if not found.
    """
    async with conn.cursor() as cur:
        await cur.execute(GET_ONE_MOOD, (id_, mood))
        row = await cur.fetchone()
    if row is None:
        return None
    return enums.TestMysqlTypesMood(row[0])


async def get_one_tag(conn: asyncmy.Connection, *, id_: int) -> enums.TestMysqlTypesTag | None:
    """Fetch one from the db using the SQL query with `name: GetOneTag :one`.

    ```sql
    SELECT tag FROM test_mysql_types WHERE id = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.

    Returns:
        Result of type `enums.TestMysqlTypesTag` fetched from the db. Will be `None` if not found.
    """
    async with conn.cursor() as cur:
        await cur.execute(GET_ONE_TAG, (id_,))
        row = await cur.fetchone()
    if row is None:
        return None
    return enums.TestMysqlTypesTag(row[0])


def get_many_date(conn: asyncmy.Connection, *, id_: int, date_test: datetime.date) -> QueryResults[datetime.date]:
    """Fetch many from the db using the SQL query with `name: GetManyDate :many`.

    ```sql
    SELECT date_test FROM test_mysql_types WHERE id = %s AND date_test = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.
        date_test: datetime.date.

    Returns:
        Helper class of type `QueryResults[datetime.date]` that allows both iteration and normal fetching of data from the db.
    """
    return QueryResults(conn, GET_MANY_DATE, operator.itemgetter(0), id_, date_test)


def get_many_time(conn: asyncmy.Connection, *, id_: int, time_test: datetime.timedelta) -> QueryResults[datetime.timedelta]:
    """Fetch many from the db using the SQL query with `name: GetManyTime :many`.

    ```sql
    SELECT time_test FROM test_mysql_types WHERE id = %s AND time_test = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.
        time_test: datetime.timedelta.

    Returns:
        Helper class of type `QueryResults[datetime.timedelta]` that allows both iteration and normal fetching of data from the db.
    """
    return QueryResults(conn, GET_MANY_TIME, operator.itemgetter(0), id_, time_test)


def get_many_bool(conn: asyncmy.Connection, *, id_: int, tinyint1_test: bool) -> QueryResults[bool]:
    """Fetch many from the db using the SQL query with `name: GetManyBool :many`.

    ```sql
    SELECT tinyint1_test FROM test_mysql_types WHERE id = %s AND tinyint1_test = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.
        tinyint1_test: bool.

    Returns:
        Helper class of type `QueryResults[bool]` that allows both iteration and normal fetching of data from the db.
    """

    def _decode_hook(row: tuple[typing.Any, ...]) -> bool:
        return bool(row[0])

    return QueryResults(conn, GET_MANY_BOOL, _decode_hook, id_, tinyint1_test)


def get_many_decimal(conn: asyncmy.Connection, *, id_: int, decimal_test: decimal.Decimal) -> QueryResults[decimal.Decimal]:
    """Fetch many from the db using the SQL query with `name: GetManyDecimal :many`.

    ```sql
    SELECT decimal_test FROM test_mysql_types WHERE id = %s AND decimal_test = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.
        decimal_test: decimal.Decimal.

    Returns:
        Helper class of type `QueryResults[decimal.Decimal]` that allows both iteration and normal fetching of data from the db.
    """
    return QueryResults(conn, GET_MANY_DECIMAL, operator.itemgetter(0), id_, decimal_test)


def get_many_mood(conn: asyncmy.Connection, *, mood: enums.TestMysqlTypesMood) -> QueryResults[enums.TestMysqlTypesMood]:
    """Fetch many from the db using the SQL query with `name: GetManyMood :many`.

    ```sql
    SELECT mood FROM test_mysql_types WHERE mood = %s ORDER BY id
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        mood: enums.TestMysqlTypesMood.

    Returns:
        Helper class of type `QueryResults[enums.TestMysqlTypesMood]` that allows both iteration and normal fetching of data from the db.
    """

    def _decode_hook(row: tuple[typing.Any, ...]) -> enums.TestMysqlTypesMood:
        return enums.TestMysqlTypesMood(row[0])

    return QueryResults(conn, GET_MANY_MOOD, _decode_hook, mood)


def list_months(conn: asyncmy.Connection) -> QueryResults[str]:
    """Fetch many from the db using the SQL query with `name: ListMonths :many`.

    ```sql
    SELECT DATE_FORMAT(datetime_test, '%%Y-%%m') AS month FROM test_mysql_types ORDER BY id
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.

    Returns:
        Helper class of type `QueryResults[str]` that allows both iteration and normal fetching of data from the db.
    """
    return QueryResults(conn, LIST_MONTHS, operator.itemgetter(0))


async def count_mysql_types(conn: asyncmy.Connection) -> int | None:
    """Fetch one from the db using the SQL query with `name: CountMysqlTypes :one`.

    ```sql
    SELECT count(*) FROM test_mysql_types
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.

    Returns:
        Result of type `int` fetched from the db. Will be `None` if not found.
    """
    async with conn.cursor() as cur:
        await cur.execute(COUNT_MYSQL_TYPES)
        row = await cur.fetchone()
    if row is None:
        return None
    return row[0]


async def update_varchar_test(conn: asyncmy.Connection, *, varchar_test: str, id_: int) -> int:
    """Execute SQL query with `name: UpdateVarcharTest :execrows` and return the number of affected rows.

    ```sql
    UPDATE test_mysql_types SET varchar_test = %s WHERE id = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        varchar_test: str.
        id_: int.

    Returns:
        The number (`int`) of affected rows. This will be 0 for queries like `CREATE TABLE`.
    """
    async with conn.cursor() as cur:
        return await cur.execute(UPDATE_VARCHAR_TEST, (varchar_test, id_))


async def delete_one_mysql_type(conn: asyncmy.Connection, *, id_: int) -> None:
    """Execute SQL query with `name: DeleteOneMysqlType :exec`.

    ```sql
    DELETE FROM test_mysql_types WHERE id = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.
    """
    async with conn.cursor() as cur:
        await cur.execute(DELETE_ONE_MYSQL_TYPE, (id_,))


async def all_mysql_types_cursor(conn: asyncmy.Connection) -> asyncmy.cursors.Cursor:
    """Execute and return the result of SQL query with `name: AllMysqlTypesCursor :execresult`.

    ```sql
    SELECT id, int_test, integer_test, mediumint_test, smallint_test, tinyint_test, bigint_test, int_unsigned_test, bigint_unsigned_test, year_test, tinyint1_test, bool_test, boolean_test, float_test, double_test, double_precision_test, real_test, decimal_test, numeric_test, char_test, varchar_test, tinytext_test, text_test, mediumtext_test, longtext_test, binary_test, varbinary_test, tinyblob_test, blob_test, mediumblob_test, longblob_test, bit_test, date_test, datetime_test, datetime6_test, timestamp_test, time_test, json_test, mood, tag FROM test_mysql_types
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.

    Returns:
        The result of type `asyncmy.cursors.Cursor` returned when executing the query.
    """
    cur = conn.cursor()
    await cur.execute(ALL_MYSQL_TYPES_CURSOR)
    return cur


async def insert_exec_last_id(conn: asyncmy.Connection, *, name: str) -> int | None:
    """Execute SQL query with `name: InsertExecLastId :execlastid` and return the id of the last affected row.

    ```sql
    INSERT INTO test_execlastid (name) VALUES (%s)
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        name: str.

    Returns:
        The id (`int`) of the last affected row. Will be `None` if no rows are affected.
    """
    async with conn.cursor() as cur:
        await cur.execute(INSERT_EXEC_LAST_ID, (name,))
        return cur.lastrowid or None


async def get_exec_last_id_name(conn: asyncmy.Connection, *, id_: int) -> str | None:
    """Fetch one from the db using the SQL query with `name: GetExecLastIdName :one`.

    ```sql
    SELECT name FROM test_execlastid WHERE id = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.

    Returns:
        Result of type `str` fetched from the db. Will be `None` if not found.
    """
    async with conn.cursor() as cur:
        await cur.execute(GET_EXEC_LAST_ID_NAME, (id_,))
        row = await cur.fetchone()
    if row is None:
        return None
    return row[0]


async def insert_type_override(conn: asyncmy.Connection, *, id_: int, text_test: UserString | None) -> None:
    """Execute SQL query with `name: InsertTypeOverride :exec`.

    ```sql
    INSERT INTO test_type_override (id, text_test) VALUES (%s, %s)
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.
        text_test: UserString | None.
    """
    async with conn.cursor() as cur:
        await cur.execute(INSERT_TYPE_OVERRIDE, (id_, str(text_test) if text_test is not None else None))


async def get_type_override(conn: asyncmy.Connection, *, id_: int) -> models.TestTypeOverride | None:
    """Fetch one from the db using the SQL query with `name: GetTypeOverride :one`.

    ```sql
    SELECT id, text_test FROM test_type_override WHERE id = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.

    Returns:
        Result of type `models.TestTypeOverride` fetched from the db. Will be `None` if not found.
    """
    async with conn.cursor() as cur:
        await cur.execute(GET_TYPE_OVERRIDE, (id_,))
        row = await cur.fetchone()
    if row is None:
        return None
    return models.TestTypeOverride(id_=row[0], text_test=UserString(row[1]) if row[1] is not None else None)


async def get_reserved_arg(conn: asyncmy.Connection, *, conn_2: str) -> models.TestReservedArg | None:
    """Fetch one from the db using the SQL query with `name: GetReservedArg :one`.

    ```sql
    SELECT id, conn FROM test_reserved_args WHERE conn = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        conn_2: str.

    Returns:
        Result of type `models.TestReservedArg` fetched from the db. Will be `None` if not found.
    """
    async with conn.cursor() as cur:
        await cur.execute(GET_RESERVED_ARG, (conn_2,))
        row = await cur.fetchone()
    if row is None:
        return None
    return models.TestReservedArg(id_=row[0], conn=row[1])


async def insert_reserved_arg(conn: asyncmy.Connection, *, id_: int, conn_2: str) -> None:
    """Execute SQL query with `name: InsertReservedArg :exec`.

    ```sql
    INSERT INTO test_reserved_args (id, conn) VALUES (%s, %s)
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        id_: int.
        conn_2: str.
    """
    async with conn.cursor() as cur:
        await cur.execute(INSERT_RESERVED_ARG, (id_, conn_2))


async def touch_exec_last_id(conn: asyncmy.Connection, *, name: str, id_: int) -> int | None:
    """Execute SQL query with `name: TouchExecLastId :execlastid` and return the id of the last affected row.

    ```sql
    UPDATE test_execlastid SET name = %s WHERE id = %s
    ```

    Args:
        conn:
            Connection object of type `asyncmy.Connection` used to execute the query.
        name: str.
        id_: int.

    Returns:
        The id (`int`) of the last affected row. Will be `None` if no rows are affected.
    """
    async with conn.cursor() as cur:
        await cur.execute(TOUCH_EXEC_LAST_ID, (name, id_))
        return cur.lastrowid or None
